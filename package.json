{
  "name": "@privchat/sdk",
  "version": "0.1.0-alpha.0",
  "description": "TypeScript SDK for PrivChat \u2014 typed FlatBuffers protocol on top of @msgtrans/client",
  "type": "module",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    },
    "./cache-idb": {
      "types": "./dist/cache-idb.d.ts",
      "import": "./dist/cache-idb.js"
    }
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "codegen": "node scripts/codegen.mjs",
    "prebuild": "npm run codegen",
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "example:accounts": "npm run codegen && tsx examples/accounts/src/main.ts",
    "example:reconnect": "npm run build && node examples/reconnect-resume/rc2-resume-sync.mjs",
    "example:transfer": "npm run codegen && tsx examples/transfer/src/main.ts"
  },
  "dependencies": {
    "dexie": "^4.4.2",
    "flatbuffers": "^24.3.25",
    "lossless-json": "^4.3.0"
  },
  "peerDependencies": {
    "@msgtrans/client": "^0.5.0-beta.0"
  },
  "devDependencies": {
    "@msgtrans/client": "file:../msgtrans-typescript",
    "@types/node": "^20.11.0",
    "fake-indexeddb": "^6.2.5",
    "tsx": "^4.21.0",
    "typescript": "^5.4.0",
    "vitest": "^1.6.0"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "Apache-2.0",
  "keywords": [
    "privchat",
    "im",
    "instant-messaging",
    "flatbuffers",
    "websocket"
  ],
  "sideEffects": [
    "./dist/api-methods.js"
  ],
  "typesVersions": {
    "*": {
      "cache-idb": [
        "./dist/cache-idb.d.ts"
      ]
    }
  }
}
